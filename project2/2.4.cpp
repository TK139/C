#include<stdio.h>
int main()
{
	printf("%d\n", 1 < 2); //1为真命题
	printf("%d", 1 > 2);   //0为假
	getchar();

	printf("%d", 2 <= 0 <= 10); //这个命题明显是假的，但结果为真
	//c的逻辑思维非常死板，计算机思维，从上到下，从左到右
	//他先算2<=0,然后是错误的是0，然后算0<=10，是正确的所以结果是1

	//  ||或，一真为真
	//  &&与，一假为假
	return 0;
}